# Astro for the static site build

The site rebuild needed a static site generator. We chose Astro (over Eleventy, the runner-up Mayur knows) for its component model, image optimization (`astro:assets` with responsive `<Picture>` variants), and typed content collections. Static output, no adapter.
