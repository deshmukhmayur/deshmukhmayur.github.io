{
  "name": "deshmukhmayur-contact-worker",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute contact-inbox --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250913.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.42.0"
  }
}
